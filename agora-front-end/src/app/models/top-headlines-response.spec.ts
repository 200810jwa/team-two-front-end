import { TopHeadlinesResponse } from './top-headlines-response';

describe('TopHeadlinesResponse', () => {
  it('should create an instance', () => {
    expect(new TopHeadlinesResponse()).toBeTruthy();
  });
});
