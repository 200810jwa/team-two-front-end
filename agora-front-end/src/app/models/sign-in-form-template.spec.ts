import { SignInFormTemplate } from './sign-in-form-template';

describe('SignInFormTemplate', () => {
  it('should create an instance', () => {
    expect(new SignInFormTemplate()).toBeTruthy();
  });
});
